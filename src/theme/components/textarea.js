const textarea = {
  baseStyle: {
    _placeholder: { color: "#58678C" },
  },
  sizes: {
    xs: {
      borderRadius: "none",
    },
    sm: {
      borderRadius: "none",
    },
    md: {
      borderRadius: "none",
      fontSize: "16px",
    },
    lg: {
      borderRadius: "none",
    },
  },
  defaultProps: {
    focusBorderColor: "#8E79FC",
  },
}

export default textarea
